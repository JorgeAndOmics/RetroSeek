# testthat scaffold for workflow/scripts/plot2sort.R
#
# Phase 1 placeholder. Real assertions land in Phase 5.3 (density
# plotter does not error on empty input).

suppressMessages({
  library(testthat)
})

test_that("plot2sort test scaffold is wired", {
  expect_true(TRUE)
})
