# testthat scaffold for workflow/scripts/species_segmenter.R
#
# Phase 1 placeholder. Real assertions land in Phase 5.3 (per-script
# regression backfill — main probe partitioning on a 5-row tibble).

suppressMessages({
  library(testthat)
})

test_that("species_segmenter test scaffold is wired", {
  expect_true(TRUE)
})
