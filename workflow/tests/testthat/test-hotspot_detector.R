# testthat scaffold for workflow/scripts/hotspot_detector.R
#
# Phase 1 placeholder. Real assertions land in Phase 5.3 (window
# aggregation respects last partial tile).

suppressMessages({
  library(testthat)
})

test_that("hotspot_detector test scaffold is wired", {
  expect_true(TRUE)
})
