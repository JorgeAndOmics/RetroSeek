# testthat scaffold for workflow/scripts/pair_detector.R
#
# Phase 1 placeholder. Real assertions land in Phase 5.3 (one row per
# ENV–GAG pair within max_gap).

suppressMessages({
  library(testthat)
})

test_that("pair_detector test scaffold is wired", {
  expect_true(TRUE)
})
